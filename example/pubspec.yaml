name: flutter_tree_demo

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.1.3
  flutter_searchable_treeview:
    path: ..

flutter:
  uses-material-design: true
